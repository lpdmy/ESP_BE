name: Build and Deploy EduShpere API to EC2

on:
  push:
    branches:
      - dev
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Setup .NET 8
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      # 3️⃣ Build & Publish Project
      - name: Build and Publish
        run: |
          dotnet restore ./EduShpere/EduShpere.csproj
          dotnet publish ./EduShpere/EduShpere.csproj -c Release -o ./publish
          
      # ----------------------------------------------------------------------
      # 4️⃣ TRUYỀN FILE LÊN EC2 (Sử dụng SCP Action)
      # ----------------------------------------------------------------------
      - name: Copy published files to EC2 Temp Directory
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_PUBLIC_IP }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          source: "./publish/"         # Thư mục đã publish trên GitHub Runner
          target: "/tmp/esp-deployment" # Thư mục tạm trên EC2

      # ----------------------------------------------------------------------
      # 5️⃣ KIỂM TRA LỖI (Diagnostics)
      # Xác minh file DLL quan trọng có tồn tại trong thư mục tạm trên EC2
      # ----------------------------------------------------------------------
      - name: 🔎 Verify DLL exists on EC2
        uses: appleboy/ssh-action@v0.1.9
        with:
          host: ${{ secrets.EC2_PUBLIC_IP }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          script: |
            TARGET_FILE="/tmp/esp-deployment/EduShpere.dll"
            
            echo "Checking if ${TARGET_FILE} exists after SCP..."
            
            # Sử dụng test -f để kiểm tra sự tồn tại và đưa ra thông báo rõ ràng
            if test -f "${TARGET_FILE}"; then
              echo "✅ Success: EduShpere.dll found in temp directory."
              ls -l "${TARGET_FILE}"
            else
              echo "❌ ERROR: EduShpere.dll NOT found in temp directory. Deployment stopped."
              exit 1 # Buộc pipeline thất bại nếu file quan trọng không có
            fi
            
      # ----------------------------------------------------------------------
      # 6️⃣ THỰC THI LỆNH TRIỂN KHAI
      # ----------------------------------------------------------------------
      - name: Run Deployment Commands on EC2
        uses: appleboy/ssh-action@v0.1.9
        with:
          host: ${{ secrets.EC2_PUBLIC_IP }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          port: 22
          script: |
            # 📚 Định nghĩa biến
            DEPLOY_DIR="/var/www/esp-api"
            BACKUP_DIR="/var/www/esp-api-backup"
            TEMP_DIR="/tmp/esp-deployment"
            APP_USER="ec2-user"
            SERVICE_NAME="espapi"
            
            # 1️⃣ Tạo folder deploy nếu chưa có + backup
            sudo mkdir -p ${DEPLOY_DIR}
            sudo mkdir -p ${BACKUP_DIR}
            
            # Backup thư mục cũ trước khi ghi đè
            if [ -d "${DEPLOY_DIR}" ]; then
              echo "✅ Backing up old deployment..."
              sudo cp -r ${DEPLOY_DIR} ${BACKUP_DIR}/$(date +%F_%H-%M-%S)
            fi

            # 2️⃣ Stop service trước khi deploy
            echo "🛑 Stopping service ${SERVICE_NAME}..."
            sudo systemctl stop ${SERVICE_NAME} || true

            # 3️⃣ Di chuyển/Đồng bộ file đã truyền vào thư mục đích (rsync cục bộ)
            echo "🔄 Synchronizing files to deployment directory..."
            sudo rsync -av --delete ${TEMP_DIR}/ ${DEPLOY_DIR}/
            
            # 4️⃣ Chỉnh quyền an toàn cho folder
            echo "🔒 Setting directory permissions..."
            sudo chown -R ${APP_USER}:${APP_USER} ${DEPLOY_DIR}
            sudo chmod -R 755 ${DEPLOY_DIR}

            # 5️⃣ Xóa thư mục tạm đã truyền file
            sudo rm -rf ${TEMP_DIR}

            # 6️⃣ Start service lại & kiểm tra trạng thái
            echo "🚀 Starting service ${SERVICE_NAME}..."
            sudo systemctl start ${SERVICE_NAME}
            sudo systemctl status ${SERVICE_NAME} --no-pager

            # 7️⃣ Kiểm tra port 5000 listen (Thêm || true để không fail pipeline)
            echo "🔍 Checking port 5000 status..."
            sudo ss -tulwn | grep 5000 || true
